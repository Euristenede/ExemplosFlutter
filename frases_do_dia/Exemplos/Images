import 'package:flutter/material.dart';

void main() {
  return runApp(MaterialApp(
      debugShowCheckedModeBanner: false,
      title: "Frases do Dia",
      home: Container(
        //color: Colors.white,
        margin: EdgeInsets.only(top: 0), //Interno
        decoration:
            BoxDecoration(border: Border.all(width: 3, color: Colors.white)),

        /*
         *contain, cover, fill, fitHeight
         *fitWidth, none, scaleDown
         */
        child: Image.asset(
          "images/mesa.jpg",
          fit: BoxFit.scaleDown,
        ),
      )));
}
